import React from 'react';
import Layout from "../../components/Layout";

const UserPage = () => {
    return (
     <Layout nickName={'dma'} id={1}>
         <div>user page</div>
     </Layout>
    );
};

export default UserPage;
